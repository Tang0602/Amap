完成部分eval文件的重新编写

1. 通用 Prompt 模板（放在每个 eval_X.py 顶部）
为了确保 AI 能吐出你想要的答案，Prompt 必须包含：模拟数据（上下文） + 任务说明 + 强制格式。
模板如下：
code
Python
"""
【上下文数据】
这里填入该任务需要的事实数据（例如：酒店名、地址、电话、收藏夹内容等）。

【任务】
这里填入具体的指令（例如：告诉我周边最近的酒店名字）。

【输出要求】
1. 请直接给出答案，不要解释原因。
2. 必须把最终答案放在 <ans> 和 </ans> 标签之间。
"""
2. 宽容型校验逻辑（通用代码）
这是一个经过优化的 validate 函数，它不要求全匹配，只要 AI 的回答中包含了你预设的关键词，就算通过。
code
Python
import logging
import re

def validate(result=None, **kwargs):
    """
    宽容型校验器：
    1. 即使 AI 没加标签，只要全文包含关键词也算过。
    2. 检查结果是否包含预设的所有关键词。
    """
    # 【这里修改每个脚本对应的预期关键词，可以是多个】
    EXPECTED_KEYWORDS = ["关键词1", "关键词2"] 

    if not result or "final_message" not in result:
        return False

    final_msg = str(result["final_message"])

    # 1. 提取检测范围（有标签看标签，没标签看全文）
    tag_match = re.search(r"<ans>\s*(.*?)\s*</ans>", final_msg, re.IGNORECASE | re.DOTALL)
    text_to_check = tag_match.group(1).strip() if tag_match else final_msg.strip()

    # 2. 关键词检索（宽容比对）
    # 只要 EXPECTED_KEYWORDS 里的每一个词都在 text_to_check 中，就返回 True
    found_all = all(kw in text_to_check for kw in EXPECTED_KEYWORDS)

    if found_all:
        logging.info(f"✅ 检测成功！匹配到关键信息: {EXPECTED_KEYWORDS}")
        return True
    else:
        logging.error(f"❌ 检测失败！回复中缺少关键信息。预期: {EXPECTED_KEYWORDS}")
        return False
3. 各个任务的具体内容清单（对应修改 EXPECTED_KEYWORDS）
你可以根据下表快速填充你的各个 eval_X.py：
任务脚本	顶部的指令（任务描述部分）	脚本内的 EXPECTED_KEYWORDS
eval_4.py	告诉我周边最近的酒店名字	["帅府铂颂饭店"] 
eval_5.py	告诉我收藏夹收藏了几处地点	[2个、2项、两个、两项]
eval_11.py	告诉我步行去最近的酒店需要几分钟	[8 或 八]
eval_12.py	告诉我八七会议会址纪念馆一天的开放时间有几个小时	[8 或 八]
eval_13.py	告诉我M+购物中心的地址	[湖北省武汉市江汉区江汉路187号]
eval_14.py	告诉我周边酒店排行榜第一的地点的电话号码	[027-83456789]
eval_15.py	告诉我收藏的第一行饭店的名称	[肖记公安牛肉鱼杂馆]
eval_22.py	告诉我台北路公共停车场停车收费标准为一个小时多少钱	[4 或 四]
eval_23.py	告诉我江汉大学（汉口校区）周边美食排行榜第一名是什么	[Smile and Salad]
eval_24.py	告诉我距离武汉市公安局（江岸分局）的周边美食排行榜第一名驾车需要几分钟	[8 或 八]