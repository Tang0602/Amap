我现在要做agent指令检测，检验逻辑是在项目启动时，会预设多个内部存储的json文件，目前是在人工演练阶段，当我做出一些动作时，该json文件会自动更新信息，然后代码会去检测这个json文件是否有期望的键值对信息，以此来判断ai是否完成任务，而我要你做的，则是完成检测脚本的编写

以下是我设计的五个简单指令：

告诉我美食排行榜中评分最高的美食
告诉我最近一次导航去了哪个地点
告诉我账号的名字和id
告诉我周边最近的酒店名字
告诉我收藏夹收藏了几个地点
修改我的名字为123456
导航去M+购物中心
打开明亮模式
删除最近的一次历史路线导航记录
收藏周边最近的餐馆
告诉我步行去最近的酒店需要几分钟
告诉我八七会议会址纪念馆的开放时间有几个小时
告诉我M+购物中心的地址
告诉我美食排行榜第一的地点的电话号码
告诉我收藏的第一行饭店的名称
步行导航去周边最近的美食店
从M+购物中心导航到我的位置
在导航去滨江饭店的路线中添加途经点群芳园
拨打周边景点排行榜第一的景点电话
收藏所有周边1km以内（包括1km）的所有景点
告诉我最近的一家四星级酒店名字
告诉我台北路公共停车场停车收费标准
告诉我江汉大学（汉口校区）周边美食排行榜第一名是什么
告诉我距离 武汉市公安局（江岸分局）的周边美食排行榜第一名步行需要多久
骑行导航去我收藏的饭店中最近的一家
步行导航去我最近去过的一家餐馆
在导航去滨江饭店的路线中添加收藏中第一个地点作为途径点
在导航去M+购物中心的路线中添加第一个途经点芦苇滩，第二个途经点武汉市人民政府
完成路线导航：M+购物中心到武汉市人民政府再到芦苇滩最后到我的位置的路线导航


你的第一步是完成五个json内部存储的命名编写，格式为1_highest_score_food,以此类推，json文件要标准化，并且还要保证项目一启动这些内部存储文件都预设并且存在，**待我确认后开启下一步检测脚本的编写**

如下是检测脚本示例：
"""
Android App Navigation Log Verification Script

This script verifies navigation actions recorded by the Android app
by reading the private storage JSON file using ADB.
"""

import json
import subprocess
import sys


def verify_last_navigation_log1(expected_action="点击消息页面", result=None, device_id=None):
    """
    Verify the last navigation log entry matches the expected action.

    Args:
        expected_action (str): The expected action value to verify

    Returns:
        bool: True if verification passes, False otherwise
    """
    try:
        # Use ADB to read the private file content directly into memory
        cmd = ["adb"]
        if device_id:
            cmd.extend(["-s", device_id])
        cmd.extend(["exec-out", "run-as", "com.example.GaoDe", "cat", "files/1_message_history.json"])
        result1 = subprocess.run(cmd, capture_output=True, text=False, check=True)

        # Decode the output with proper encoding handling
        try:
            stdout_text = result1.stdout.decode("utf-8")
        except UnicodeDecodeError:
            # Fallback to gbk encoding for Chinese Windows systems
            try:
                stdout_text = result1.stdout.decode("gbk")
            except UnicodeDecodeError:
                # Last resort: ignore decode errors
                stdout_text = result1.stdout.decode("utf-8", errors="ignore")

        # Parse JSON content from memory
        if not stdout_text.strip():
            print("FAIL: JSON file is empty")
            return False

        json_data = json.loads(stdout_text)

        # Check if there are any records
        if not json_data or len(json_data) == 0:
            print("FAIL: No records found in JSON file")
            return False

        # Get the last record
        last_record = json_data[-1]

        # Verify the action field
        if "action" not in last_record:
            print("FAIL: 'action' field not found in last record")
            return False

        actual_action = last_record["action"]
        if actual_action == expected_action:
            print("PASS: Navigation action verification successful")
            print(f"Expected: {expected_action}")
            print(f"Actual: {actual_action}")
            print(f"Timestamp: {last_record.get('timestamp', 'N/A')}")
            print(f"Page: {last_record.get('page', 'N/A')}")
            return True
        else:
            print("FAIL: Action mismatch")
            print(f"Expected: {expected_action}")
            print(f"Actual: {actual_action}")
            return False

    except subprocess.CalledProcessError as e:
        print(f"FAIL: ADB command failed - {e}")
        try:
            error_text = e.stderr.decode("utf-8") if e.stderr else "No error output"
        except:
            error_text = "Error decoding stderr"
        print(f"Error output: {error_text}")
        return False
    except json.JSONDecodeError as e:
        print(f"FAIL: JSON parsing error - {e}")
        print(f"Raw content: {stdout_text}")
        return False
    except Exception as e:
        print(f"FAIL: Unexpected error - {e}")
        return False


if __name__ == "__main__":
    # Verify the expected operation: "点击消息页面"
    expected_action = "点击消息页面"
    success = verify_last_navigation_log1(expected_action)

    if success:
        print("\n✓ PASS")
        sys.exit(0)
    else:
        print("\n✗ FAIL")
        sys.exit(1)
